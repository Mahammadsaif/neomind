import AppLayout from "@/components/layout/AppLayout";

export default function SettingsPage() {
  return (
    <AppLayout>
      <h1 className="text-2xl font-semibold mb-2">Settings</h1>
      <p className="text-muted">Your preferences and account settings.</p>
    </AppLayout>
  );
}